import React from "react";
import { Wrapper, Input, Button } from "./style";

function CommentBox() {
  return (
    <Wrapper>
      <Input></Input>
      <Button>입력</Button>
    </Wrapper>
  );
}

export default CommentBox;
