import styled from "styled-components";

export const Aside = styled.aside`
  position: fixed;
  font-size: 18px;
  padding: 0;
  margin: 0;
`;

export const Index = styled.ul`
  font-size: 18px;
  padding: 0;
`;
